[package]
name = "denv"
version = "0.1.5"
edition = "2024"

[dependencies]
# none

[dev-dependencies]
proptest = "1"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
strip = "symbols"
panic = "abort"
